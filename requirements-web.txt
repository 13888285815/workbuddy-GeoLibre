# GeoLibre Web UI Requirements

# FastAPI framework
fastapi>=0.104.0

# ASGI server
uvicorn[standard]>=0.24.0

# File upload support
python-multipart>=0.0.6

# HTTP client for testing
requests>=2.31.0

# CORS support (included in FastAPI)
# starlette (automatically installed with FastAPI)

# Optional: Pydantic for data validation (included in FastAPI)
# pydantic>=2.0.0 (automatically installed)
